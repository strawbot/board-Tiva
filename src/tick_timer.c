// tick_timer.c — RETIRED
//
// Timer0A initialisation, the 1 ms ISR, and tick_ms() have all moved to
// src/clocks.c as part of the TimbreOS port.  This file is kept empty so
// the build system does not error on the source path; it will be removed
// in a future cleanup once all references are gone.
